# work.md

2026-06-30 16:40:02 +08:00 --- 发现 MVP 边界 [MVP Boundary] 在 P0 范围与最终验收优先级之间存在范围不一致 --- 使用 `grill-with-docs` 澄清并按推荐答案固化为项目词汇表 [Project Glossary] --- 修改了 `CONTEXT.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `CONTEXT.md`，并从 `work.md` 删除本条记录；若 `work.md` 仅包含本条记录，可直接删除 `work.md`。

2026-06-30 16:41:53 +08:00 --- 发现“管理系统 [Admin System]”可能被误解为包含新增、编辑、删除 [CRUD]，但 MVP 验收标准 [Acceptance Criteria] 只要求关键读路径 [Critical Read Paths] --- 使用 `grill-with-docs` 澄清并固化“只读 MVP [Read-only MVP]”术语 --- 修改了 `CONTEXT.md`、`work.md`。撤回方式 [Rollback Strategy]：从 `CONTEXT.md` 删除“只读 MVP [Read-only MVP]”小节，并从 `work.md` 删除本条记录。

2026-06-30 16:48:05 +08:00 --- 发现 Drizzle schema 与既有 Northwind PostgreSQL 表之间需要明确所有权边界 [Schema Ownership Boundary]，否则未来可能误执行破坏性迁移 [Destructive Migrations] --- 使用 ADR [Architecture Decision Record] 记录数据库优先 [Database-first] 决策，并创建 `docs/adr/` 目录；2026-06-30 16:48:29 +08:00 按全局沟通规则 [Global Communication Rules] 将 ADR 文本修正为中文双语对照 [Bilingual Mapping] --- 修改了 `docs/adr/0001-database-first-northwind-schema.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `docs/adr/0001-database-first-northwind-schema.md`；若 `docs/adr/` 为空，可删除该目录；并从 `work.md` 删除本条记录。

2026-06-30 16:52:09 +08:00 --- 发现 Products、Customers、Orders 等页面需要关联名称、金额合计、订单状态 [Order Status] 等派生数据 [Derived Data]，必须明确前后端聚合边界 [Aggregation Boundary] --- 使用 ADR [Architecture Decision Record] 记录后端聚合读取 DTO [Backend-aggregated Read DTOs] 决策 --- 修改了 `docs/adr/0002-backend-aggregates-read-dtos.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `docs/adr/0002-backend-aggregates-read-dtos.md`，并从 `work.md` 删除本条记录。

2026-06-30 16:53:25 +08:00 --- 发现 Auth 在原始需求中属于 P1，但路由与系统表设计可能让 MVP 范围 [MVP Scope] 膨胀到真实认证 [Real Authentication] --- 使用 ADR [Architecture Decision Record] 记录本轮延后真实认证 [Defer Real Authentication]，仅保留前端占位骨架 [Placeholder Skeleton] --- 修改了 `docs/adr/0003-defer-real-authentication-for-mvp.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `docs/adr/0003-defer-real-authentication-for-mvp.md`，并从 `work.md` 删除本条记录。

2026-06-30 16:56:39 +08:00 --- 发现“完整项目 [Complete Project]”可能被理解为一次性实现全量模块，也可能被理解为完整骨架 [Project Skeleton] 加可运行闭环 [Runnable Slice] --- 使用 ADR [Architecture Decision Record] 记录采用完整工程骨架 [Project Skeleton] 加 MVP 闭环实现 [MVP Runnable Slice] 的交付方式 --- 修改了 `docs/adr/0004-project-skeleton-with-mvp-runnable-slice.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `docs/adr/0004-project-skeleton-with-mvp-runnable-slice.md`，并从 `work.md` 删除本条记录。

2026-06-30 17:10:59 +08:00 --- 发现附件 schema [Target Schema] 与真实 PostgreSQL Northwind 数据库可能存在类型差异 [Type Differences]、大小写差异 [Case Differences] 或字段差异 [Field Differences] --- 确认后续实现增加只读数据库检查脚本 [Read-only Database Inspection Script]，仅查询 `information_schema`，不修改数据库；该决策可逆且不创建 ADR [Architecture Decision Record] --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录。

2026-06-30 17:14:46 +08:00 --- 发现金额字段 [Money Fields] 在 API 中若返回数字可能受 JavaScript 浮点精度问题 [Floating Point Precision] 影响，但图表 [Charts] 又需要数值输入 --- 使用 ADR [Architecture Decision Record] 记录金额字段统一返回固定两位小数字符串 [Fixed 2-decimal String]，图表适配层 [Chart Adapter] 显式转换为数字 --- 修改了 `docs/adr/0005-money-fields-as-fixed-decimal-strings.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `docs/adr/0005-money-fields-as-fixed-decimal-strings.md`，并从 `work.md` 删除本条记录。

2026-06-30 17:17:22 +08:00 --- 发现“所有列表支持搜索 [Search]、筛选 [Filtering]、排序 [Sorting]”缺少字段级搜索语义 [Field-level Search Semantics]，可能导致前后端对 keyword 含义理解不一致 --- 确认 MVP 采用轻量关键词搜索 [Lightweight Keyword Search]：Products 搜 `product_name`，Customers 搜 `company_name` 与 `contact_name`，Orders 对 `order_id` 精确匹配 [Exact Match] 或对客户公司名 [Customer Company Name] 模糊匹配 [Partial Match]，Reports 不提供 keyword；排序字段使用后端白名单 [Backend Allowlist] --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录。

2026-06-30 17:22:20 +08:00 --- 发现 `packages/shared` 若共享过度，会把 Drizzle schema、数据库字段名 [Database Field Names] 或服务层内部类型 [Service Internal Types] 泄漏给前端，破坏 API 契约边界 [API Contract Boundary] --- 使用 ADR [Architecture Decision Record] 记录 shared 包只包含 API 契约类型 [API Contract Types]、统一响应类型 [Shared Response Types]、分页类型 [Pagination Types] 与纯工具类型 [Pure Utility Types]，并统一使用 camelCase --- 修改了 `docs/adr/0006-shared-package-contains-api-contract-types-only.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `docs/adr/0006-shared-package-contains-api-contract-types-only.md`，并从 `work.md` 删除本条记录。

2026-06-30 17:24:40 +08:00 --- 发现 MVP 若完全不写测试 [Tests] 会让金额计算 [Money Calculation]、订单状态推导 [Order Status Derivation] 与统一响应格式 [Response Shape] 缺少保护，但完整前端组件测试 [Component Tests] 会拖慢最小可运行后台 [Runnable MVP Admin] 搭建 --- 确认 MVP 配置测试工具 [Testing Tools]，只写后端关键测试 [Critical Backend Tests]：健康检查 [Health Check]、响应格式 [Response Shape]、金额计算 [Money Calculation]、订单状态推导 [Order Status Derivation]；前端暂不写组件测试 [Component Tests]，依赖 TypeScript strict 与手动验收 [Manual Acceptance] --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录。

2026-06-30 17:25:18 +08:00 --- 发现后台管理系统 [Admin System] 若采用营销站式大卡片 [Marketing-style Cards] 或装饰性界面，会降低表格扫描 [Table Scanning] 与重复操作 [Repeated Actions] 效率 --- 确认前端采用工作台式紧凑布局 [Compact Workbench Layout]：以表格 [Tables]、筛选栏 [Filter Bar]、分页 [Pagination] 与详情页 [Detail Page] 为主，Dashboard 可使用统计卡片 [Metric Cards] 与图表 [Charts]，整体保持安静、清晰、高信息密度 [High Information Density] --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录。

2026-06-30 17:37:21 +08:00 --- 发现 `/to-prd` 需要议题追踪器 [Issue Tracker]、分诊标签 [Triage Labels] 与领域文档布局 [Domain Docs Layout] 配置，但仓库缺少 `AGENTS.md` 与 `docs/agents/` --- 根据用户确认使用 GitHub Issues、默认分诊标签 [Default Triage Labels] 与单上下文 [Single-context]，创建代理配置文档 [Agent Configuration Docs]；2026-06-30 17:38:17 +08:00 将 `AGENTS.md` 摘要修正为中文双语对照 [Bilingual Mapping] --- 修改了 `AGENTS.md`、`docs/agents/issue-tracker.md`、`docs/agents/triage-labels.md`、`docs/agents/domain.md`、`work.md`。撤回方式 [Rollback Strategy]：删除 `AGENTS.md` 与 `docs/agents/` 下三个新增文件；若 `docs/agents/` 为空，可删除该目录；并从 `work.md` 删除本条记录。

2026-06-30 17:42:59 +08:00 --- 发现 `/to-prd` 需要把已确认的最小可运行后台 [Runnable MVP Admin] 决策发布到议题追踪器 [Issue Tracker]，且 GitHub 仓库缺少 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 分诊标签 [Triage Labels] --- 使用 GitHub CLI 创建缺失标签，并发布 PRD Issue `#1`：`https://github.com/marskim1130/northwind-admin/issues/1`，已应用 `ready-for-agent` 标签；PRD 覆盖问题陈述 [Problem Statement]、解决方案 [Solution]、用户故事 [User Stories]、实现决策 [Implementation Decisions]、测试决策 [Testing Decisions]、不在范围内 [Out of Scope] 与进一步说明 [Further Notes] --- 修改了 GitHub Issues 与 GitHub Labels，并修改了 `work.md`。撤回方式 [Rollback Strategy]：关闭或删除 GitHub Issue `#1`；如不再需要，删除新增标签 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`；并从 `work.md` 删除本条记录。

2026-06-30 17:47:37 +08:00 --- 发现 PRD Issue `#1` 需要拆成可独立交付的垂直切片 [Vertical Slices]，避免水平分层 [Horizontal Slicing] 导致不可验收任务 --- 使用 `/to-issues` 将 PRD 拆为 7 个 AFK GitHub Issues，并按依赖顺序发布：`#2` 可运行工程骨架与健康检查 [Runnable Skeleton + Health Check]、`#3` Products 商品读路径 [Products Read Path]、`#4` Customers 客户读路径 [Customers Read Path]、`#5` Orders 订单读路径 [Orders Read Path]、`#6` Dashboard 经营概览 [Dashboard Overview]、`#7` Reports 报表中心 [Reports Center]、`#8` MVP 验收收束与文档 [MVP Acceptance + Documentation]；所有 Issue 均已应用 `ready-for-agent` 标签 --- 修改了 GitHub Issues，并修改了 `work.md`。撤回方式 [Rollback Strategy]：关闭或删除 GitHub Issues `#2` 到 `#8`，并从 `work.md` 删除本条记录；不要关闭父 PRD Issue `#1`，除非要撤回整个 PRD。

2026-06-30 17:54:32 +08:00 --- 发现仓库只有文档与议题配置 [Issue Configuration]，还没有可运行工程骨架 [Runnable Project Skeleton]；用户要求先初始化项目 [Project Initialization] --- 创建 pnpm workspace [Workspace]、根配置、`apps/api`、`apps/web`、`packages/shared`、`.env.example`、README 初稿、只读数据库检查脚本 [Read-only Database Inspection Script]、Vue 工作台外壳 [Workbench Shell]，并写入第一个 RED 测试 [Failing Test]：`GET /api/health` 应返回统一成功响应 [Unified Success Response]；未实现 `/api/health`，保留红 [RED] 状态；运行 `pnpm install` 生成 `pnpm-lock.yaml`；类型检查 [Type Check] 发现 TypeScript 6 弃用 `baseUrl`，已移除暂未使用的前端路径别名 [Path Alias]；2026-06-30 17:59:08 +08:00 清理前端未使用图标导入 [Unused Import]，`pnpm typecheck` 已通过 --- 修改了 `package.json`、`pnpm-workspace.yaml`、`pnpm-lock.yaml`、`tsconfig.base.json`、`.gitignore`、`.env.example`、`README.md`、`packages/shared/*`、`apps/api/*`、`apps/web/*`、`work.md`。撤回方式 [Rollback Strategy]：删除 `package.json`、`pnpm-workspace.yaml`、`pnpm-lock.yaml`、`tsconfig.base.json`、`.gitignore`、`.env.example`、`README.md`、`apps/`、`packages/`，并从 `work.md` 删除本条记录。

2026-07-02 09:49:51 +08:00 --- 用户确认第一个 RED 测试 [Failing Test] 失败，`GET /api/health` 返回 404 而不是 200 --- 进入绿 [GREEN] 阶段，只添加最小 Hono 健康检查路由 [Health Check Route]，返回统一成功响应 [Unified Success Response]；2026-07-02 09:50:30 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/src/app.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `/api/health` 路由，并从 `work.md` 删除本条记录。

2026-07-02 09:53:04 +08:00 --- 进入第二个 RED 阶段 [RED Phase]，需要保护基础错误响应 [Error Response] 的公共 HTTP 行为 [Public HTTP Behavior] --- 只新增一个失败测试 [Failing Test]：未知 API 路由应返回 404 JSON 统一错误响应 [Unified Error Response]，未实现 notFound 处理器 [Not Found Handler] --- 修改了 `apps/api/src/app.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.test.ts` 删除“错误响应 [Error Response]”测试块，并从 `work.md` 删除本条记录。

2026-07-02 09:56:17 +08:00 --- 用户确认第二个 RED 测试 [Failing Test] 失败，未知 API 路由返回 `text/plain` 而不是 JSON 统一错误响应 [Unified Error Response] --- 进入绿 [GREEN] 阶段，只添加最小 Hono notFound 处理器 [Not Found Handler]，返回 `NOT_FOUND` 错误对象与 404 状态码；2026-07-02 09:56:51 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/src/app.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `app.notFound` 处理器，并从 `work.md` 删除本条记录。

2026-07-02 10:05:41 +08:00 --- 进入第三个 RED 阶段 [RED Phase]，需要保护只读数据库检查脚本 [Read-only Database Inspection Script] 在缺少 `DATABASE_URL` 时的公共命令行为 [Public Command Behavior] --- 只新增一个失败测试 [Failing Test]：运行 `pnpm db:inspect` 且 `DATABASE_URL` 为空时，应以状态码 1 失败并输出清晰配置错误 [Clear Configuration Error]，不输出堆栈前缀 [Stack Trace Prefix] `Error:`；未修改脚本实现；2026-07-02 10:07:24 +08:00 修正测试启动 pnpm 的方式 [Test Harness]，改用当前进程的 `npm_execpath` 避免 Windows shell 警告 [Shell Warning]，确认 RED 失败点来自脚本输出堆栈 [Stack Trace] --- 修改了 `apps/api/src/inspect-db.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/inspect-db.test.ts`，并从 `work.md` 删除本条记录。

2026-07-02 10:09:00 +08:00 --- 用户要求继续下一步，第三个 RED 测试 [Failing Test] 已确认脚本在缺少 `DATABASE_URL` 时输出 `Error:` 与堆栈 [Stack Trace] --- 进入绿 [GREEN] 阶段，只修改只读数据库检查脚本 [Read-only Database Inspection Script] 的 catch 分支 [Catch Branch]，输出错误消息 [Error Message] 而不是完整 Error 对象 [Error Object]；2026-07-02 10:09:34 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/scripts/inspect-db.ts`、`work.md`。撤回方式 [Rollback Strategy]：将 `apps/api/scripts/inspect-db.ts` 的 catch 分支恢复为 `console.error(error)`，并从 `work.md` 删除本条记录。

2026-07-02 10:12:06 +08:00 --- 进入第四个 RED 阶段 [RED Phase]，需要保护后端环境变量校验 [Environment Validation] 行为，避免 `API_PORT` 非数字时被静默接受 --- 只新增一个失败测试 [Failing Test]：`API_PORT` 为 `not-a-number` 时，`loadEnv()` 应抛出清晰配置错误 [Clear Configuration Error]；未修改环境加载实现 [Environment Loading Implementation] --- 修改了 `apps/api/src/env.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/env.test.ts`，并从 `work.md` 删除本条记录。

2026-07-02 10:14:32 +08:00 --- 用户要求继续下一步，第四个 RED 测试 [Failing Test] 已确认 `API_PORT=not-a-number` 时 `loadEnv()` 不抛错 --- 进入绿 [GREEN] 阶段，只为环境加载 [Environment Loading] 增加最小 `API_PORT` 校验 [Environment Validation]：允许缺省值 [Default Value]，但非整数或超出 1 到 65535 的端口范围 [Port Range] 时抛出清晰配置错误 [Clear Configuration Error]；2026-07-02 10:15:04 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/src/env.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/env.ts` 删除 `validateApiPort` 调用和函数，并从 `work.md` 删除本条记录。

2026-07-02 10:19:18 +08:00 --- 进入第五个 RED 阶段 [RED Phase]，需要保护 `/api/health` 响应形状 [Response Shape] 与 shared API 契约类型 [API Contract Types] 不发生契约漂移 [Contract Drift] --- 只修改现有健康检查测试 [Health Check Test]，将期望响应标注为 `SuccessResponse<HealthCheckData>`；当前 shared 包尚未导出 `HealthCheckData`，预期 `pnpm typecheck` 失败 --- 修改了 `apps/api/src/app.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：移除 `apps/api/src/app.test.ts` 中的 `HealthCheckData` 类型导入与 `expectedBody` 契约标注，并从 `work.md` 删除本条记录。

2026-07-02 10:21:26 +08:00 --- 第五个 RED 阶段 [RED Phase] 已确认 `pnpm typecheck` 因 shared 包缺少 `HealthCheckData` 导出而失败 --- 进入绿 [GREEN] 阶段，只在 shared API 契约类型 [API Contract Types] 中新增 `HealthCheckData`，匹配 `/api/health` 当前响应数据 [Response Data] `{ status: \"OK\" }`；2026-07-02 10:22:02 +08:00 验证 `pnpm typecheck` 与 `pnpm test:api` 均通过 --- 修改了 `packages/shared/src/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `packages/shared/src/index.ts` 删除 `HealthCheckData` 接口，并从 `work.md` 删除本条记录。

2026-07-02 09:29:50 +08:00 --- 发现项目已有 `DATABASE_URL` 环境变量 [Environment Variable] 约定，但本地缺少真实 PostgreSQL 连接配置 [PostgreSQL Connection Configuration] --- 根据用户提供的 `psql -h 118.25.138.138 -p 5432 -U postgres -d postgres` 命令创建本地 `.env`，配置主机 [Host]、端口 [Port]、用户 [User] 与数据库 [Database]；未写入密码 [Password]，避免明文泄漏 [Plaintext Secret Leakage] --- 修改了 `.env`、`work.md`。撤回方式 [Rollback Strategy]：删除 `.env`，并从 `work.md` 删除本条记录。

2026-07-02 09:31:06 +08:00 --- 发现 `pnpm db:inspect` 在 `apps/api` 工作目录 [Working Directory] 运行，默认 `dotenv` 未加载仓库根目录 `.env`，导致 `DATABASE_URL` 缺失 --- 新增 API 环境加载 helper [Helper]，让 API 入口 [API Entry] 与只读数据库检查脚本 [Read-only Database Inspection Script] 统一读取根目录 `.env`；同时配置 Vite `envDir`，让前端 [Frontend] 读取同一个根目录环境变量 [Environment Variables] --- 修改了 `apps/api/src/env.ts`、`apps/api/src/index.ts`、`apps/api/scripts/inspect-db.ts`、`apps/web/vite.config.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/env.ts`；把 `apps/api/src/index.ts` 与 `apps/api/scripts/inspect-db.ts` 恢复为直接调用 `dotenv.config()`；从 `apps/web/vite.config.ts` 删除 `envDir` 与 `node:url` 导入；并从 `work.md` 删除本条记录。

2026-07-02 10:52:05 +08:00 --- 发现健康检查 [Health Check] 与 404 notFound 仍在 `app.ts` 中手写统一响应对象 [Unified Response Objects]，后续 Products/Customers/Orders 会重复响应结构 --- 在现有绿灯测试 [Green Tests] 保护下进行重构 [Refactor]，新增 `ok()` / `fail()` 响应 helper [Response Helper]，并让 `/api/health` 与 notFound 复用 helper；行为不变；2026-07-02 10:52:40 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/src/response.ts`、`apps/api/src/app.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/response.ts`，并把 `apps/api/src/app.ts` 恢复为直接手写成功/错误响应对象；从 `work.md` 删除本条记录。

2026-07-02 10:53:04 +08:00 --- 进入数据库检查摘要 [Database Inspection Summary] 的 RED 阶段 [RED Phase]，需要让只读数据库检查脚本 [Read-only Database Inspection Script] 能基于 `information_schema` 行报告已发现表数量 [Found Table Count] 与缺失表 [Missing Tables] --- 只新增一个失败测试 [Failing Test]：导入尚不存在的 `summarizeInspection()` 并断言 `products` 被发现、`orders` 缺失；未实现模块 --- 修改了 `apps/api/src/db-inspection.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/db-inspection.test.ts`，并从 `work.md` 删除本条记录。

2026-07-02 10:53:34 +08:00 --- 数据库检查摘要 [Database Inspection Summary] RED 已确认失败，原因是 `./db-inspection` 模块不存在 --- 进入绿 [GREEN] 阶段，只新增 `summarizeInspection()` 最小实现 [Minimal Implementation]，根据 `information_schema` 行计算已发现表数量 [Found Table Count] 与缺失表 [Missing Tables]；2026-07-02 10:54:04 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/src/db-inspection.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/db-inspection.ts`，并从 `work.md` 删除本条记录。

2026-07-02 10:54:21 +08:00 --- 发现 `inspect-db.ts` 与新建 `summarizeInspection()` 存在重复的已发现表/缺失表计算 [Duplicate Summary Logic] --- 在现有测试保护下进行重构 [Refactor]，让只读数据库检查脚本 [Read-only Database Inspection Script] 复用 `summarizeInspection()`，保持命令输出不变；2026-07-02 10:54:53 +08:00 验证 `pnpm test:api`、`pnpm typecheck`、`pnpm db:inspect` 均可运行，其中 `db:inspect` 仍报告 `public` schema 下目标 Northwind 表缺失 [Missing Tables] --- 修改了 `apps/api/scripts/inspect-db.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/scripts/inspect-db.ts` 删除 `summarizeInspection` 导入与行映射，恢复脚本内直接用 `Set` 计算 `foundTables`/`missingTables`，并从 `work.md` 删除本条记录。

2026-07-02 10:55:12 +08:00 --- 进入数据库字段检查 [Database Column Inspection] 的 RED 阶段 [RED Phase]，需要只读数据库检查脚本 [Read-only Database Inspection Script] 能报告表存在但字段缺失 [Missing Columns] 的情况 --- 只新增一个失败测试 [Failing Test]：`products` 表存在但缺少 `unit_price` 字段时，`summarizeInspection()` 应输出缺失字段列表 [Missing Columns List]；未修改实现 --- 修改了 `apps/api/src/db-inspection.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/db-inspection.test.ts` 删除“表存在但字段缺失时报告缺失字段 [Missing Columns]”测试，并从 `work.md` 删除本条记录。

2026-07-02 10:55:46 +08:00 --- 数据库字段检查 [Database Column Inspection] RED 已确认失败，`summary.missingColumns` 尚未实现 --- 进入绿 [GREEN] 阶段，只为 `summarizeInspection()` 增加可选 `requiredColumnsByTable` 输入与缺失字段列表 [Missing Columns List] 输出；缺失表 [Missing Tables] 不重复展开字段缺失；2026-07-02 10:56:21 +08:00 验证 `pnpm test:api` 与 `pnpm typecheck` 均通过 --- 修改了 `apps/api/src/db-inspection.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/db-inspection.ts` 删除 `requiredColumnsByTable`、`missingColumns` 与字段集合计算逻辑，并从 `work.md` 删除本条记录。

2026-07-02 10:56:39 +08:00 --- 发现 `summarizeInspection()` 已能报告缺失字段 [Missing Columns]，但 `pnpm db:inspect` 尚未传入目标字段清单 [Target Column List]，无法在真实库检查中展示字段差异 [Field Differences] --- 将 Northwind 目标字段清单接入只读数据库检查脚本 [Read-only Database Inspection Script]，由字段清单推导目标表 [Required Tables]，并输出 `Missing columns`；仍只查询 `information_schema`，不读取业务数据、不修改数据库 --- 修改了 `apps/api/scripts/inspect-db.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/scripts/inspect-db.ts` 删除 `requiredColumnsByTable` 与 `Missing columns` 输出，恢复原 `requiredTables` 数组，并从 `work.md` 删除本条记录。

2026-07-02 10:57:39 +08:00 --- 验证 Issue #2 工程骨架 [Project Skeleton] 当前状态，准备进入数据库门禁 [Database Gate] 与后续 Products 读路径 [Products Read Path] --- 运行 `pnpm test:api` 通过 6 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过，运行 `pnpm build` 通过；运行 `pnpm db:inspect` 时数据库连接 [Database Connection] 在连接阶段返回 `Connection terminated unexpectedly`，因此真实 Northwind 表检查 [Real Schema Inspection] 暂时无法通过，进入 Products 前需要修正 `.env`/数据库目标 [Database Target] --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:20:48 +08:00 --- 用户提供新的 `DATABASE_URL` 后，`psql` 可连接 `northwind/public`，但 Node `pg` 驱动 [Driver] 连接返回 `Connection terminated unexpectedly`；临时验证 `postgres.js` 可用同一连接串执行只读 `select current_database()` --- 将 API 的 PostgreSQL 客户端依赖 [PostgreSQL Client Dependency] 增加 `postgres@3.4.9`，并把只读数据库检查脚本 [Read-only Database Inspection Script] 从 `pg` 切换为 `postgres.js` 参数化查询 [Parameterized Query]；首次 `ANY(${sql.array(...)})` 触发 text array 类型错误，已改为 `IN ${sql(requiredTables)}` 参数化列表 [Parameterized List]；确认代码无 `pg` 引用后移除 `pg` 与 `@types/pg` 依赖；2026-07-02 11:22:38 +08:00 验证 `pnpm test:api`、`pnpm typecheck`、`pnpm db:inspect` 均通过，`db:inspect` 检查到 14 张表且无缺失表/字段 --- 修改了 `apps/api/package.json`、`pnpm-lock.yaml`、`apps/api/scripts/inspect-db.ts`、`work.md`。撤回方式 [Rollback Strategy]：重新添加 `pg` 与 `@types/pg` 依赖，移除 `postgres` 依赖，恢复 `apps/api/scripts/inspect-db.ts` 使用 `pg.Client` 与 `$1` 参数，并从 `work.md` 删除本条记录。

2026-07-02 11:23:07 +08:00 --- 进入 Issue #3 Products 商品读路径 [Products Read Path] 的第一个 RED 阶段 [RED Phase]，需要通过公共 HTTP 接口 [Public HTTP Interface] 验证默认分页商品列表 [Paginated Products List] --- 只新增一个失败测试 [Failing Test]：`GET /api/products` 应返回 200、统一成功响应 [Success Response]、默认 `page=1`、`pageSize=20`、`total > 0`，并包含商品列表关键字段 [Key Fields]；未实现路由 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/products.test.ts`，并从 `work.md` 删除本条记录。

2026-07-02 11:23:54 +08:00 --- Products 默认分页列表 [Paginated Products List] RED 已确认失败，`GET /api/products` 返回 404 --- 进入绿 [GREEN] 阶段，只实现最小 Products 只读列表 [Read-only List]：从真实 PostgreSQL 查询 `products`、`categories`、`suppliers`，返回默认 `page=1`、`pageSize=20`、`total`、商品关键字段 [Key Fields]、两位小数字符串金额 [Fixed 2-decimal String] 与低库存标记 [Low Stock Flag]；首次运行时远程数据库 [Remote Database] 查询超过 Vitest 默认 5 秒，已将该真实库集成测试 [Integration Test] 超时调高到 20 秒；暂不实现搜索/筛选/排序参数 --- 修改了 `apps/api/src/products.service.ts`、`apps/api/src/app.ts`、`apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/products.service.ts`，从 `apps/api/src/app.ts` 删除 `/api/products` 路由和 `listProducts` 导入，从 `apps/api/src/products.test.ts` 移除 20 秒超时参数，并从 `work.md` 删除本条记录。

2026-07-02 11:26:31 +08:00 --- 验证 Products 默认分页列表 [Paginated Products List] 的 GREEN 状态，确认真实数据库门禁 [Database Gate] 未回退 --- 运行 `pnpm test:api` 通过 7 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过，运行 `pnpm db:inspect` 检查到 14 张表且无缺失表/字段 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:26:47 +08:00 --- 进入 Products 分页查询参数 [Pagination Query Params] 的 RED 阶段 [RED Phase]，需要公共 HTTP 接口 [Public HTTP Interface] 支持 `page` 与 `pageSize` 控制分页响应 [Paginated Response] --- 只新增一个失败测试 [Failing Test]：`GET /api/products?page=2&pageSize=5` 应返回 `page=2`、`pageSize=5` 且 5 条商品；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“支持分页查询参数 [Pagination Query Params]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:27:40 +08:00 --- Products 分页查询参数 [Pagination Query Params] RED 已确认失败，`GET /api/products?page=2&pageSize=5` 仍返回 `page=1` --- 进入绿 [GREEN] 阶段，只将路由层 [Route Layer] 的 `page` 与 `pageSize` 查询参数传入 Products 只读查询 [Read-only Query]，并用它们计算 `limit` 与 `offset`；暂不实现参数校验 [Validation]、搜索 [Search]、筛选 [Filtering] 或排序 [Sorting] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 恢复 `/api/products` 路由为无参数调用 `listProducts()`；从 `apps/api/src/products.service.ts` 删除 `ListProductsOptions` 并恢复固定 `page=1`、`pageSize=20`、`offset=0`；从 `work.md` 删除本条记录。

2026-07-02 11:28:35 +08:00 --- 验证 Products 分页查询参数 [Pagination Query Params] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 8 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:28:47 +08:00 --- 进入 Products 关键词搜索 [Keyword Search] 的 RED 阶段 [RED Phase]，需要 `GET /api/products?keyword=...` 只搜索 `product_name` --- 只新增一个失败测试 [Failing Test]：`keyword=chai` 时返回成功响应 [Success Response]，`total > 0`，且每个 `productName` 都包含 `chai`；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“按商品名关键词搜索 [Product Name Keyword Search]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:30:02 +08:00 --- Products 关键词搜索 [Keyword Search] RED 已确认失败，`keyword=chai` 时默认列表中存在不匹配商品；同次运行还暴露远程数据库 [Remote Database] 偶发导致分页测试超时 [Timeout] --- 进入绿 [GREEN] 阶段，只将 `keyword` 查询参数传入 Products 查询，并对 `p.product_name` 使用大小写不敏感匹配 [Case-insensitive Match]；总数 [Total Count] 与列表 [Items] 使用同一条件 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `keyword` 参数传递；从 `apps/api/src/products.service.ts` 删除 `keyword` 选项、`keywordFilter` 与两个 `where ${keywordFilter}`；从 `work.md` 删除本条记录。

2026-07-02 11:30:56 +08:00 --- 验证 Products 关键词搜索 [Keyword Search] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 9 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:31:09 +08:00 --- 进入 Products 分类筛选 [Category Filter] 的 RED 阶段 [RED Phase]，需要 `GET /api/products?categoryId=1` 只返回指定分类商品 --- 只新增一个失败测试 [Failing Test]：`categoryId=1` 时返回成功响应 [Success Response]、`total > 0`，且每个 `categoryName` 都为 `Beverages`；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“按分类筛选 [Category Filter]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:32:57 +08:00 --- Products 分类筛选 [Category Filter] RED 运行时受到远程数据库 [Remote Database] 偶发超时 [Timeout] 干扰，未得到干净断言失败；直接调用 `listProducts({ page: 1, pageSize: 5 })` 验证 SQL 查询本身约 220ms，可继续最小实现 --- 进入绿 [GREEN] 阶段，只将 `categoryId` 查询参数传入 Products 查询，并追加 `p.category_id = categoryId` 过滤条件 [Filter Condition]；暂不实现其他筛选 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `categoryId` 参数传递；从 `apps/api/src/products.service.ts` 删除 `categoryId` 选项、`categoryFilter` 与两个 `and ${categoryFilter}`；从 `work.md` 删除本条记录。

2026-07-02 11:34:35 +08:00 --- 发现 Products 真实库集成测试 [Integration Tests] 与 `inspect-db` 子进程测试 [Subprocess Test] 并行执行时可能造成远程数据库 [Remote Database] 偶发超时 [Timeout]，且 Products 测试文件内存在重复 per-test timeout 配置 --- 在当前绿灯行为 [Green Behavior] 下做测试稳定性重构 [Test Stability Refactor]：按 Vitest 4.1 官方配置 [Official Config] 设置 `fileParallelism: false` 与全局 `testTimeout: 30000`，并移除 Products 测试中的重复超时参数；不改变断言 --- 修改了 `apps/api/vitest.config.ts`、`apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/vitest.config.ts` 删除 `fileParallelism` 与 `testTimeout`；把 `apps/api/src/products.test.ts` 中 4 个 Products 测试恢复为第三参数 `20000`；从 `work.md` 删除本条记录。

2026-07-02 11:35:16 +08:00 --- 验证 Products 分类筛选 [Category Filter] 与测试稳定性重构 [Test Stability Refactor] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 10 个 API 测试 [API Tests]，串行配置后耗时约 3.57 秒；运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:35:29 +08:00 --- 进入 Products 供应商筛选 [Supplier Filter] 的 RED 阶段 [RED Phase]，需要 `GET /api/products?supplierId=1` 只返回指定供应商商品 --- 只新增一个失败测试 [Failing Test]：`supplierId=1` 时返回成功响应 [Success Response]、`total > 0`，且每个 `supplierName` 都为 `Exotic Liquids`；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“按供应商筛选 [Supplier Filter]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:36:15 +08:00 --- Products 供应商筛选 [Supplier Filter] RED 已确认失败，`supplierId=1` 时列表仍包含其他供应商商品 --- 进入绿 [GREEN] 阶段，只将 `supplierId` 查询参数传入 Products 查询，并追加 `p.supplier_id = supplierId` 过滤条件 [Filter Condition]；暂不实现停产 [Discontinued] 或库存 [Stock] 筛选 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `supplierId` 参数传递；从 `apps/api/src/products.service.ts` 删除 `supplierId` 选项、`supplierFilter` 与两个 `and ${supplierFilter}`；从 `work.md` 删除本条记录。

2026-07-02 11:39:32 +08:00 --- 发现 Products 测试虽然可通过，但每个请求都新建并关闭远程 PostgreSQL 连接 [Remote PostgreSQL Connection]，导致测试耗时不稳定且 GREEN 验证偶发超时 [Timeout] --- 在当前行为测试 [Behavior Tests] 保护下做连接池重构 [Connection Pool Refactor]：新增 `getDb()`/`closeDb()`，按 Postgres.js 官方配置 [Official Options] 设置 `max=1`、`idle_timeout=5`、`connect_timeout=10`，Products 服务复用连接池，Products 测试结束后显式关闭连接 --- 修改了 `apps/api/src/db.ts`、`apps/api/src/products.service.ts`、`apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/db.ts`；恢复 `apps/api/src/products.service.ts` 为直接导入 `postgres` 与 `loadEnv`、每次请求创建客户端并在 `finally` 中 `sql.end({ timeout: 5 })`；从 `apps/api/src/products.test.ts` 删除 `afterAll` 与 `closeDb` 导入/调用；从 `work.md` 删除本条记录。

2026-07-02 11:41:32 +08:00 --- 验证 Products 供应商筛选 [Supplier Filter] 与连接池重构 [Connection Pool Refactor] 的 GREEN 状态；直接只读查询 [Read-only Query] 还发现真实库 `discontinued` 字段为整数 [Integer] `1/0`，非布尔 [Boolean] --- 运行 `pnpm test:api` 通过 11 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过；直接调用 `listProducts({ supplierId: 1 })` 返回 2 条 `Exotic Liquids` 商品 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:41:48 +08:00 --- 进入 Products 停产筛选 [Discontinued Filter] 的 RED 阶段 [RED Phase]，需要 `GET /api/products?discontinued=true` 将 API 布尔语义 [Boolean Semantics] 映射到真实库整数 [Integer] 字段 `1/0` --- 只新增一个失败测试 [Failing Test]：`discontinued=true&pageSize=20` 时返回 10 条固定停产商品 ID；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“按停产状态筛选 [Discontinued Filter]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:42:32 +08:00 --- Products 停产筛选 [Discontinued Filter] RED 已确认失败，`discontinued=true` 仍返回全部 77 条商品 --- 进入绿 [GREEN] 阶段，只将 `discontinued` 查询参数解析为布尔 [Boolean]，并在 Products 查询中映射为 `p.discontinued = 1/0`；路由处理器 [Route Handler] 改为块级函数以避免重复读取该参数 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `discontinued` 解析并恢复 Products 路由为直接表达式处理器；从 `apps/api/src/products.service.ts` 删除 `discontinued` 选项、`discontinuedFilter` 与两个 `and ${discontinuedFilter}`；从 `work.md` 删除本条记录。

2026-07-02 11:43:35 +08:00 --- 验证 Products 停产筛选 [Discontinued Filter] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 12 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:43:48 +08:00 --- 进入 Products 低库存筛选 [Low Stock Filter] 的 RED 阶段 [RED Phase]，需要 `GET /api/products?lowStock=true` 只返回低库存商品 --- 只新增一个失败测试 [Failing Test]：`lowStock=true&pageSize=100` 时返回成功响应 [Success Response]、`total > 0`，且每个响应项的 `lowStock` 均为 `true`；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“按低库存筛选 [Low Stock Filter]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:44:39 +08:00 --- Products 低库存筛选 [Low Stock Filter] RED 已确认失败，`lowStock=true` 时返回项中仍存在 `lowStock=false` --- 进入绿 [GREEN] 阶段，只将 `lowStock` 查询参数解析为布尔 [Boolean]，并映射为库存阈值条件 [Stock Threshold Condition]：`true` 使用 `p.units_in_stock <= p.reorder_level`，`false` 使用相反条件 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `lowStock` 解析与参数传递；从 `apps/api/src/products.service.ts` 删除 `lowStock` 选项、`lowStockFilter` 与两个 `and ${lowStockFilter}`；从 `work.md` 删除本条记录。

2026-07-02 11:45:37 +08:00 --- 验证 Products 低库存筛选 [Low Stock Filter] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 13 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:45:50 +08:00 --- 进入 Products 单价排序 [Unit Price Sort] 的 RED 阶段 [RED Phase]，需要 `GET /api/products?sortBy=unitPrice&sortOrder=desc` 通过 API 字段名 [API Field Name] 安全映射到数据库字段 [Database Field] --- 只新增一个失败测试 [Failing Test]：取前 5 条商品时 `unitPrice` 应按降序 [Descending] 排列；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“按单价降序排序 [Unit Price Desc Sort]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:46:35 +08:00 --- Products 单价排序 [Unit Price Sort] RED 已确认失败，`sortBy=unitPrice&sortOrder=desc` 仍按 `product_id` 默认顺序返回 --- 进入绿 [GREEN] 阶段，只新增服务层排序白名单 [Sorting Allowlist]，将 `productId`、`productName`、`categoryName`、`supplierName`、`unitPrice` 映射到固定 SQL 片段 [SQL Fragments]，并支持 `desc`/默认 `asc`；暂不实现非法 `sortBy` 的错误响应 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `sortBy` 与 `sortOrder` 参数传递；从 `apps/api/src/products.service.ts` 删除排序选项、`sortFields`、`sortField`、`sortDirection` 并恢复 `order by p.product_id asc`；从 `work.md` 删除本条记录。

2026-07-02 11:47:29 +08:00 --- 验证 Products 单价排序 [Unit Price Sort] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 14 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:47:42 +08:00 --- 进入 Products 排序白名单校验 [Sorting Allowlist Validation] 的 RED 阶段 [RED Phase]，需要非法 `sortBy` 在进入数据库前返回统一错误响应 [Unified Error Response] --- 只新增一个失败测试 [Failing Test]：`sortBy=dropTable` 应返回 400、`INVALID_SORT_BY` 与允许字段列表；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“拒绝非法排序字段 [Invalid Sort Field]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:48:17 +08:00 --- Products 排序白名单校验 [Sorting Allowlist Validation] RED 已确认失败，非法 `sortBy=dropTable` 仍返回 200 --- 进入绿 [GREEN] 阶段，将 Products 排序字段白名单 [Sort Field Allowlist] 集中为 `productSortByFields` 与 `isProductSortBy()`，路由层 [Route Layer] 在查询数据库前返回 400 `INVALID_SORT_BY` 统一错误响应 [Unified Error Response] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `isProductSortBy`/`productSortByFields` 导入与非法 `sortBy` 分支；从 `apps/api/src/products.service.ts` 删除导出的白名单与类型守卫 [Type Guard]，恢复 `sortBy?: string` 和服务内 fallback 判断；从 `work.md` 删除本条记录。

2026-07-02 11:49:17 +08:00 --- 非法排序字段 [Invalid Sort Field] 的 API 测试 [API Test] 已通过，但 `pnpm typecheck` 发现路由层类型窄化 [Type Narrowing] 未传递到 `listProducts()` 参数，`sortBy` 仍被视为普通字符串 [String] --- 保持运行行为不变，新增局部变量 `productSortBy` 保存 `isProductSortBy()` 类型守卫后的值，再传入服务层 [Service Layer] --- 修改了 `apps/api/src/app.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `productSortBy` 局部变量，恢复直接传递 `sortBy`；从 `work.md` 删除本条记录。

2026-07-02 11:49:59 +08:00 --- 验证 Products 排序白名单校验 [Sorting Allowlist Validation] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 15 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:50:11 +08:00 --- 进入 Products 详情接口 [Product Detail Endpoint] 的 RED 阶段 [RED Phase]，需要 `GET /api/products/:id` 返回单个商品详情 [Product Detail] --- 只新增一个失败测试 [Failing Test]：`GET /api/products/1` 应返回 Chai 的关键字段 [Key Fields] 与统一成功响应 [Success Response]；未修改实现 --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/products.test.ts` 删除“返回商品详情 [Product Detail]”测试，并从 `work.md` 删除本条记录。

2026-07-02 11:50:43 +08:00 --- Products 详情接口 [Product Detail Endpoint] RED 已确认失败，`GET /api/products/1` 返回 404 --- 进入绿 [GREEN] 阶段，新增 `getProductById()` 只读查询 [Read-only Query]，提取 `mapProductRow()` 复用列表/详情映射 [Mapping]，并新增 `/api/products/:id` 路由返回统一成功响应 [Success Response]；未新增缺失商品测试 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/products.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `/api/products/:id` 路由与 `getProductById` 导入；从 `apps/api/src/products.service.ts` 删除 `getProductById()` 与 `mapProductRow()`，恢复列表内联映射；从 `work.md` 删除本条记录。

2026-07-02 11:51:34 +08:00 --- Products 详情接口 [Product Detail Endpoint] GREEN 首次验证时返回 200，但测试数据假设 [Test Data Assumption] 错误：真实库中 `productId=1` 的供应商名称 [Supplier Name] 为 `Specialty Biscuits, Ltd.`，不是经典样例中的 `Exotic Liquids` --- 只修正详情测试 [Detail Test] 的供应商期望值，不修改业务代码 [Business Code] --- 修改了 `apps/api/src/products.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：把 `apps/api/src/products.test.ts` 中 Chai 的 `supplierName` 期望恢复为 `Exotic Liquids`，并从 `work.md` 删除本条记录。

2026-07-02 11:53:17 +08:00 --- 验证 Products 详情接口 [Product Detail Endpoint] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 16 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:53:31 +08:00 --- 进入 Products 前端列表页 [Frontend Products List Page] 的 RED 阶段 [RED Phase]，需要 `/products` 从占位页 [Placeholder Page] 切换到真实商品表格页面 [Products Table Page] --- 只修改路由 [Route] 导入尚不存在的 `ProductsPage.vue` 并将 `/products` 指向它，预期 `pnpm typecheck` 因缺失组件失败；未实现页面 --- 修改了 `apps/web/src/router/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/web/src/router/index.ts` 删除 `ProductsPage` 导入，并将 `/products` 路由恢复为 `PlaceholderPage`；从 `work.md` 删除本条记录。

2026-07-02 11:54:09 +08:00 --- Products 前端列表页 [Frontend Products List Page] RED 已确认失败，`pnpm typecheck` 因缺失 `ProductsPage.vue` 报错 --- 进入绿 [GREEN] 阶段，新增 shared Products API 契约类型 [API Contract Types]、Products API 客户端 [API Client]、Products query key [Query Key] 与 `ProductsPage.vue`；页面包含筛选栏 [Filter Bar]、Element Plus 表格 [Table]、分页 [Pagination]、排序 [Sorting]、Loading/Error/Empty 状态 [States]，通过 vue-query 请求真实 `/api/products` --- 修改了 `packages/shared/src/index.ts`、`apps/web/src/api/products.ts`、`apps/web/src/api/queryKeys.ts`、`apps/web/src/pages/products/ProductsPage.vue`、`apps/web/src/router/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/web/src/pages/products/ProductsPage.vue` 与 `apps/web/src/api/products.ts`；从 `packages/shared/src/index.ts` 删除 Products 相关类型；从 `apps/web/src/api/queryKeys.ts` 删除 `products` query key 与 `ProductListQuery` 导入；从 `apps/web/src/router/index.ts` 删除 `ProductsPage` 导入并恢复 `/products` 为 `PlaceholderPage`；如 `apps/web/src/pages/products/` 为空可删除该目录；从 `work.md` 删除本条记录。

2026-07-02 11:56:56 +08:00 --- 验证 Products 前端列表页 [Frontend Products List Page] 的 GREEN 状态 --- 运行 `pnpm typecheck` 通过，运行 `pnpm test:api` 通过 16 个 API 测试 [API Tests] --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:57:16 +08:00 --- 进入 Products 前端详情页 [Frontend Product Detail Page] 的 RED 阶段 [RED Phase]，需要 `/products/:productId` 从占位页 [Placeholder Page] 切换到真实详情页 [Detail Page] --- 只修改路由 [Route] 导入尚不存在的 `ProductDetailPage.vue` 并将 `/products/:productId` 指向它，预期 `pnpm typecheck` 因缺失组件失败；未实现页面 --- 修改了 `apps/web/src/router/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/web/src/router/index.ts` 删除 `ProductDetailPage` 导入，并将 `/products/:productId` 路由恢复为 `PlaceholderPage`；从 `work.md` 删除本条记录。

2026-07-02 11:57:53 +08:00 --- Products 前端详情页 [Frontend Product Detail Page] RED 已确认失败，`pnpm typecheck` 因缺失 `ProductDetailPage.vue` 报错 --- 进入绿 [GREEN] 阶段，新增只读 `ProductDetailPage.vue`，通过 vue-query 与 `fetchProduct()` 请求真实 `/api/products/:id`，展示返回列表按钮 [Back Button]、Loading/Error 状态 [States] 与基础详情 [Basic Detail] --- 修改了 `apps/web/src/pages/products/ProductDetailPage.vue`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/web/src/pages/products/ProductDetailPage.vue`；从 `work.md` 删除本条记录。路由恢复方式见前序记录。

2026-07-02 11:58:59 +08:00 --- 验证 Products 前端详情页 [Frontend Product Detail Page] 的 GREEN 状态 --- 运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:59:41 +08:00 --- 验收 Issue #3 Products 商品读路径 [Products Read Path] 当前纵切 [Vertical Slice] 状态 --- 运行 `pnpm test:api` 通过 16 个 API 测试 [API Tests]，运行 `pnpm build` 通过；前端构建 [Frontend Build] 存在依赖包 Rolldown 纯注释 [Pure Annotation] 与 chunk size 警告 [Chunk Size Warning]，但构建成功 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 11:59:57 +08:00 --- 进入 Issue #4 Customers 客户读路径 [Customers Read Path] 的第一个 RED 阶段 [RED Phase]，需要 `GET /api/customers` 返回默认分页客户列表 [Paginated Customers List] 与聚合字段 [Aggregate Fields] --- 只新增一个失败测试 [Failing Test]：默认响应应包含 `customerId`、`companyName`、`contactName`、`country`、`city`、`orderCount`、两位小数字符串 [Fixed 2-decimal String] `totalSales`；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/customers.test.ts`，并从 `work.md` 删除本条记录。

2026-07-02 12:00:42 +08:00 --- Customers 默认分页列表 [Paginated Customers List] RED 已确认失败，`GET /api/customers` 返回 404 --- 进入绿 [GREEN] 阶段，只实现最小客户只读列表 [Read-only List]：从 `customers` 左连接 [Left Join] `orders` 与 `order_details`，聚合 `orderCount` 与 `totalSales`，返回默认 `page=1`、`pageSize=20`；暂不实现搜索 [Search]、筛选 [Filtering]、排序 [Sorting] 或详情 [Detail] --- 修改了 `apps/api/src/customers.service.ts`、`apps/api/src/app.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/api/src/customers.service.ts`；从 `apps/api/src/app.ts` 删除 `listCustomers` 导入与 `/api/customers` 路由；从 `work.md` 删除本条记录。

2026-07-02 12:01:50 +08:00 --- 验证 Customers 默认分页列表 [Paginated Customers List] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 17 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:02:04 +08:00 --- 进入 Customers 分页查询参数 [Pagination Query Params] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers?page=2&pageSize=5` 控制分页响应 [Paginated Response] --- 只新增一个失败测试 [Failing Test]：响应应返回 `page=2`、`pageSize=5` 且 5 条客户；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“支持分页查询参数 [Pagination Query Params]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:02:35 +08:00 --- Customers 分页查询参数 [Pagination Query Params] RED 已确认失败，`page=2&pageSize=5` 仍返回 `page=1` --- 进入绿 [GREEN] 阶段，只将路由层 [Route Layer] 的 `page` 与 `pageSize` 查询参数传入 Customers 聚合查询 [Aggregate Query]，并用它们计算 `limit` 与 `offset`；暂不实现搜索 [Search]、筛选 [Filtering] 或排序 [Sorting] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 恢复 `/api/customers` 路由为无参数调用 `listCustomers()`；从 `apps/api/src/customers.service.ts` 删除 `ListCustomersOptions` 并恢复固定 `page=1`、`pageSize=20`、`offset=0`；从 `work.md` 删除本条记录。

2026-07-02 12:03:30 +08:00 --- 验证 Customers 分页查询参数 [Pagination Query Params] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 18 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:03:44 +08:00 --- 进入 Customers 关键词搜索 [Keyword Search] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers?keyword=...` 搜索 `company_name` 与 `contact_name` --- 只新增一个失败测试 [Failing Test]：`keyword=alfreds` 时返回成功响应 [Success Response]、`total > 0`，且每个客户的公司名或联系人包含 `alfreds`；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“公司名或联系人关键词搜索 [Company Or Contact Keyword Search]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:04:27 +08:00 --- Customers 关键词搜索 [Keyword Search] RED 已确认失败，`keyword=alfreds` 时默认客户列表中存在不匹配客户 --- 进入绿 [GREEN] 阶段，只将 `keyword` 查询参数传入 Customers 聚合查询 [Aggregate Query]，对 `c.company_name` 与 `c.contact_name` 使用大小写不敏感匹配 [Case-insensitive Match]，并让总数 [Total Count] 与列表 [Items] 使用同一过滤条件 [Filter Condition] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `keyword` 参数传递；从 `apps/api/src/customers.service.ts` 删除 `keyword` 选项、`keywordFilter` 与两个 `where ${keywordFilter}`；从 `work.md` 删除本条记录。

2026-07-02 12:05:14 +08:00 --- 验证 Customers 关键词搜索 [Keyword Search] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 19 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:05:35 +08:00 --- 进入 Customers 国家筛选 [Country Filter] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers?country=...` 只返回指定国家客户 --- 只新增一个失败测试 [Failing Test]：`country=Germany&pageSize=50` 时返回成功响应 [Success Response]、`total > 0`，且每个客户的 `country` 都为 `Germany`；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“按国家筛选 [Country Filter]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:06:20 +08:00 --- Customers 国家筛选 [Country Filter] RED 已确认失败，`country=Germany` 时列表仍包含其他国家客户 --- 进入绿 [GREEN] 阶段，只将 `country` 查询参数传入 Customers 聚合查询 [Aggregate Query]，并追加精确匹配 [Exact Match] `c.country = country`；暂不实现城市筛选 [City Filter] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `country` 参数传递；从 `apps/api/src/customers.service.ts` 删除 `country` 选项、`countryFilter` 与两个 `and ${countryFilter}`；从 `work.md` 删除本条记录。

2026-07-02 12:07:07 +08:00 --- 验证 Customers 国家筛选 [Country Filter] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 20 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:07:22 +08:00 --- 进入 Customers 城市筛选 [City Filter] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers?city=...` 只返回指定城市客户 --- 只新增一个失败测试 [Failing Test]：`city=Berlin&pageSize=50` 时返回成功响应 [Success Response]、`total > 0`，且每个客户的 `city` 都为 `Berlin`；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“按城市筛选 [City Filter]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:07:51 +08:00 --- Customers 城市筛选 [City Filter] RED 已确认失败，`city=Berlin` 时列表仍包含其他城市客户 --- 进入绿 [GREEN] 阶段，只将 `city` 查询参数传入 Customers 聚合查询 [Aggregate Query]，并追加精确匹配 [Exact Match] `c.city = city` --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `city` 参数传递；从 `apps/api/src/customers.service.ts` 删除 `city` 选项、`cityFilter` 与两个 `and ${cityFilter}`；从 `work.md` 删除本条记录。

2026-07-02 12:08:40 +08:00 --- 验证 Customers 城市筛选 [City Filter] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 21 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:08:55 +08:00 --- 进入 Customers 总销售额排序 [Total Sales Sort] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers?sortBy=totalSales&sortOrder=desc` 按聚合金额 [Aggregate Money] 排序 --- 只新增一个失败测试 [Failing Test]：取前 5 条客户时 `totalSales` 应按降序 [Descending] 排列；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“按总销售额降序排序 [Total Sales Desc Sort]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:09:31 +08:00 --- Customers 总销售额排序 [Total Sales Sort] RED 已确认失败，`sortBy=totalSales&sortOrder=desc` 仍按默认客户 ID 顺序返回 --- 进入绿 [GREEN] 阶段，只新增服务层排序白名单 [Sorting Allowlist]，将 `customerId`、`companyName`、`contactName`、`country`、`city`、`orderCount`、`totalSales` 映射到固定 SQL 片段 [SQL Fragments]，并支持 `desc`/默认 `asc`；暂不实现非法 `sortBy` 的错误响应 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 Customers 的 `sortBy` 与 `sortOrder` 参数传递；从 `apps/api/src/customers.service.ts` 删除排序选项、`sortFields`、`sortField`、`sortDirection` 并恢复 `order by c.customer_id asc`；从 `work.md` 删除本条记录。

2026-07-02 12:10:26 +08:00 --- 验证 Customers 总销售额排序 [Total Sales Sort] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 22 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:10:42 +08:00 --- 进入 Customers 排序白名单校验 [Sorting Allowlist Validation] 的 RED 阶段 [RED Phase]，需要非法 `sortBy` 在进入数据库前返回统一错误响应 [Unified Error Response] --- 只新增一个失败测试 [Failing Test]：`sortBy=dropTable` 应返回 400、`INVALID_SORT_BY` 与允许字段列表；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“拒绝非法排序字段 [Invalid Sort Field]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:11:18 +08:00 --- Customers 排序白名单校验 [Sorting Allowlist Validation] RED 已确认失败，非法 `sortBy=dropTable` 仍返回 200 --- 进入绿 [GREEN] 阶段，将 Customers 排序字段白名单 [Sort Field Allowlist] 集中为 `customerSortByFields` 与 `isCustomerSortBy()`，路由层 [Route Layer] 在查询数据库前返回 400 `INVALID_SORT_BY` 统一错误响应 [Unified Error Response] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `isCustomerSortBy`/`customerSortByFields` 导入与 Customers 非法 `sortBy` 分支，恢复直接传递 `sortBy`；从 `apps/api/src/customers.service.ts` 删除导出的白名单与类型守卫 [Type Guard]，恢复 `sortBy?: string` 和服务内 fallback 判断；从 `work.md` 删除本条记录。

2026-07-02 12:12:19 +08:00 --- 验证 Customers 排序白名单校验 [Sorting Allowlist Validation] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 23 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:12:33 +08:00 --- 进入 Customers 详情接口 [Customer Detail Endpoint] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers/:id` 返回单个客户详情 [Customer Detail] 与最近下单日期 [Recent Order Date] --- 只新增一个失败测试 [Failing Test]：`GET /api/customers/ALFKI` 应返回 Alfreds Futterkiste 的关键字段 [Key Fields]、聚合字段 [Aggregate Fields]、两位小数字符串 [Fixed 2-decimal String] `totalSales` 与 ISO 字符串 [ISO String] `recentOrderDate`；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“返回客户详情 [Customer Detail]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:13:14 +08:00 --- Customers 详情接口 [Customer Detail Endpoint] RED 已确认失败，`GET /api/customers/ALFKI` 返回 404 --- 进入绿 [GREEN] 阶段，新增 `getCustomerById()` 只读聚合查询 [Read-only Aggregate Query]，复用客户列表映射 [Mapping] 并补充 `recentOrderDate` ISO 字符串 [ISO String]，新增 `/api/customers/:id` 路由返回统一成功响应 [Success Response]；未新增缺失客户测试 --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `/api/customers/:id` 路由与 `getCustomerById` 导入；从 `apps/api/src/customers.service.ts` 删除 `getCustomerById()`、`CustomerDetailRow`、`toIsoString()` 与 `mapCustomerListRow()`，恢复列表内联映射；从 `work.md` 删除本条记录。

2026-07-02 12:14:17 +08:00 --- 验证 Customers 详情接口 [Customer Detail Endpoint] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 24 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:14:35 +08:00 --- 进入客户订单列表 [Customer Orders List] 的 RED 阶段 [RED Phase]，需要 `GET /api/customers/:id/orders` 返回指定客户订单分页列表 [Paginated Orders List] --- 只新增一个失败测试 [Failing Test]：`GET /api/customers/ALFKI/orders` 应返回 `page=1`、`pageSize=20`、`total > 0`，并包含 `orderId`、ISO 字符串 [ISO String] `orderDate`、两位小数字符串 [Fixed 2-decimal String] `orderTotal`；未修改实现 --- 修改了 `apps/api/src/customers.test.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/customers.test.ts` 删除“返回客户订单列表 [Customer Orders List]”测试，并从 `work.md` 删除本条记录。

2026-07-02 12:15:18 +08:00 --- 客户订单列表 [Customer Orders List] RED 已确认失败，`GET /api/customers/ALFKI/orders` 返回 404 --- 进入绿 [GREEN] 阶段，新增 `listCustomerOrders()` 只读聚合查询 [Read-only Aggregate Query]，按订单聚合 `order_details` 金额为两位小数字符串 [Fixed 2-decimal String] `orderTotal`，并新增 `/api/customers/:id/orders` 路由返回分页成功响应 [Paginated Success Response] --- 修改了 `apps/api/src/app.ts`、`apps/api/src/customers.service.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/api/src/app.ts` 删除 `/api/customers/:id/orders` 路由与 `listCustomerOrders` 导入；从 `apps/api/src/customers.service.ts` 删除 `CustomerOrderRow` 与 `listCustomerOrders()`；从 `work.md` 删除本条记录。

2026-07-02 12:16:25 +08:00 --- 验证客户订单列表 [Customer Orders List] 的 GREEN 状态 --- 运行 `pnpm test:api` 通过 25 个 API 测试 [API Tests]，运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:16:40 +08:00 --- 进入 Customers 前端列表页 [Frontend Customers List Page] 的 RED 阶段 [RED Phase]，需要 `/customers` 从占位页 [Placeholder Page] 切换到真实客户表格页面 [Customers Table Page] --- 只修改路由 [Route] 导入尚不存在的 `CustomersPage.vue` 并将 `/customers` 指向它，预期 `pnpm typecheck` 因缺失组件失败；未实现页面 --- 修改了 `apps/web/src/router/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/web/src/router/index.ts` 删除 `CustomersPage` 导入，并将 `/customers` 路由恢复为 `PlaceholderPage`；从 `work.md` 删除本条记录。

2026-07-02 12:17:14 +08:00 --- Customers 前端列表页 [Frontend Customers List Page] RED 已确认失败，`pnpm typecheck` 因缺失 `CustomersPage.vue` 报错 --- 进入绿 [GREEN] 阶段，新增 shared Customers API 契约类型 [API Contract Types]、Customers API 客户端 [API Client]、Customers query key [Query Key] 与 `CustomersPage.vue`；页面包含筛选栏 [Filter Bar]、Element Plus 表格 [Table]、分页 [Pagination]、排序 [Sorting]、Loading/Error/Empty 状态 [States]，通过 vue-query 请求真实 `/api/customers` --- 修改了 `packages/shared/src/index.ts`、`apps/web/src/api/customers.ts`、`apps/web/src/api/queryKeys.ts`、`apps/web/src/pages/customers/CustomersPage.vue`、`apps/web/src/router/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/web/src/pages/customers/CustomersPage.vue` 与 `apps/web/src/api/customers.ts`；从 `packages/shared/src/index.ts` 删除 Customers 相关类型；从 `apps/web/src/api/queryKeys.ts` 删除 `customers` query key 与 `CustomerListQuery` 导入；从 `apps/web/src/router/index.ts` 删除 `CustomersPage` 导入并恢复 `/customers` 为 `PlaceholderPage`；如 `apps/web/src/pages/customers/` 为空可删除该目录；从 `work.md` 删除本条记录。

2026-07-02 12:18:55 +08:00 --- 验证 Customers 前端列表页 [Frontend Customers List Page] 的 GREEN 状态 --- 运行 `pnpm typecheck` 通过 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 12:19:15 +08:00 --- 进入 Customers 前端详情页 [Frontend Customer Detail Page] 的 RED 阶段 [RED Phase]，需要 `/customers/:customerId` 从占位页 [Placeholder Page] 切换到真实详情页 [Detail Page] --- 只修改路由 [Route] 导入尚不存在的 `CustomerDetailPage.vue` 并将 `/customers/:customerId` 指向它，预期 `pnpm typecheck` 因缺失组件失败；未实现页面 --- 修改了 `apps/web/src/router/index.ts`、`work.md`。撤回方式 [Rollback Strategy]：从 `apps/web/src/router/index.ts` 删除 `CustomerDetailPage` 导入，并将 `/customers/:customerId` 路由恢复为 `PlaceholderPage`；从 `work.md` 删除本条记录。

2026-07-02 12:19:53 +08:00 --- Customers 前端详情页 [Frontend Customer Detail Page] RED 已确认失败，`pnpm typecheck` 因缺失 `CustomerDetailPage.vue` 报错 --- 进入绿 [GREEN] 阶段，新增只读 `CustomerDetailPage.vue`，通过 vue-query 请求真实 `/api/customers/:id` 与 `/api/customers/:id/orders`，用 Day.js 展示日期 [Date Formatting]，展示客户详情、聚合金额 [Aggregate Money] 与最近订单列表 [Recent Orders List] --- 修改了 `apps/web/src/pages/customers/CustomerDetailPage.vue`、`work.md`。撤回方式 [Rollback Strategy]：删除 `apps/web/src/pages/customers/CustomerDetailPage.vue`；从 `work.md` 删除本条记录。路由恢复方式见前序记录。

2026-07-02 12:21:27 +08:00 --- 验收 Issue #4 Customers 客户读路径 [Customers Read Path] 当前纵切 [Vertical Slice] 状态 --- 运行 `pnpm test:api` 通过 25 个 API 测试 [API Tests]，运行 `pnpm build` 通过；前端构建 [Frontend Build] 仍存在依赖包 Rolldown 纯注释 [Pure Annotation] 与 chunk size 警告 [Chunk Size Warning]，但构建成功 --- 修改了 `work.md`。撤回方式 [Rollback Strategy]：从 `work.md` 删除本条记录；代码修改回滚方式见前序记录。

2026-07-02 15:32:27 +08:00 --- 用户要求先提交已完成 Issue，发现工作区包含一颗尚未完成的 Orders RED 测试 [Failing Test]，不应混入已完成 Issue 提交 [Completed Issue Commit] --- 删除未完成的 `apps/api/src/orders-money.test.ts` 并移除对应 `work.md` 记录，保留已完成 Issue #2、#3、#4 的改动；提交前运行 `pnpm test:api` 通过 25 个 API 测试 [API Tests]，`pnpm typecheck` 通过，`pnpm build` 通过，构建仍仅有第三方依赖 Rolldown 纯注释 [Pure Annotation] 与 chunk size 警告 [Chunk Size Warning] --- 修改了 `work.md`，并移除未完成文件 `apps/api/src/orders-money.test.ts`。撤回方式 [Rollback Strategy]：如需恢复 Orders RED，重新新增 `apps/api/src/orders-money.test.ts` 与对应 `work.md` 记录；如需撤回本条记录，从 `work.md` 删除本条。
